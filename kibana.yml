server.name: kibana
server.host: "0.0.0.0"
elasticsearch.hosts: "http://quickstart-es-http:9200"
xpack.monitoring.ui.container.elasticsearch.enabled: true
elasticsearch.username: ${ELASTICSEARCH_USERNAME}
elasticsearch.password: ${ELASTICSEARCH_PASSWORD}
